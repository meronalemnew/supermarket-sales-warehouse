# Dataset

This project uses a public supermarket sales dataset with 1,000 transactions from three branches.

I renamed the original column headers to snake_case before loading the data into PostgreSQL.

The dataset itself is not included in this repository. Download the source dataset before running the project.