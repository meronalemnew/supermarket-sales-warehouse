name: supermarket_dbt
version: "1.0.0"
config-version: 2

profile: supermarket_dbt

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  supermarket_dbt:
    staging:
      +materialized: view
    marts:
      +materialized: table